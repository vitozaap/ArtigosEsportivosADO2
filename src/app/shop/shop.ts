import { Component } from '@angular/core';
import { Header } from '../components/header/header';

@Component({
  selector: 'app-shop',
  imports: [Header],
  templateUrl: './shop.html',
})
export class Shop { }
